import { PlaceholderPage } from "@/components/placeholder-page";

export default function LoginPage() {
  return (
    <PlaceholderPage
      eyebrow="Account access"
      title="Log in is not active yet."
      description="Authentication is intentionally outside this first skeleton phase. The future login will let registered users access saved signal reports."
      status="Authentication arrives in Phase 03"
      privatePreview
    />
  );
}
