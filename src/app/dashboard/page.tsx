import { PlaceholderPage } from "@/components/placeholder-page";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      eyebrow="Personal dashboard"
      title="Your saved signal reports will live here."
      description="Once authentication and saved reports are implemented, this workspace will help users return to the drug signal snapshots they are monitoring."
      status="Saved reports arrive in Phase 08"
      privatePreview
    />
  );
}
